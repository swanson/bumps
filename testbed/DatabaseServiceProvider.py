

class DatabaseServiceProvider():
    def __init__(self, db):
        self.db = db
        self.cursor = self.db.cursor

    def decodePIDValue(bytes):
        print bytes

    def logToDatabase(self, data):
        self.decodePIDValue()
        print "add %s to db" % data
